#a program using function to print multiplication table of a given number till the square of the number
def multiply(n):
    for i in range(1,n+1):
        print(f"{n}*{i}={n*i}")
        
n=int(input("Enter the number"))  
multiply(n)
